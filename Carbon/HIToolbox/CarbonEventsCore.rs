//! This file has been automatically generated by `objc2`'s `header-translator`.
//! DO NOT EDIT
use core::cell::UnsafeCell;
use core::ffi::*;
use core::marker::{PhantomData, PhantomPinned};
use core::ptr::NonNull;
#[cfg(feature = "objc2")]
use objc2::__framework_prelude::*;
use objc2_core_foundation::*;
#[cfg(feature = "objc2-core-graphics")]
use objc2_core_graphics::*;

use crate::*;

/// [Apple's documentation](https://developer.apple.com/documentation/carbon/event?language=objc)
#[doc(alias = "EventRef")]
#[repr(C)]
#[derive(Debug)]
pub struct Event {
    inner: [u8; 0],
    _p: UnsafeCell<PhantomData<(*const UnsafeCell<()>, PhantomPinned)>>,
}

#[cfg(feature = "objc2")]
unsafe impl RefEncode for Event {
    const ENCODING_REF: Encoding = Encoding::Pointer(&Encoding::Struct("OpaqueEventRef", &[]));
}

/// [Apple's documentation](https://developer.apple.com/documentation/carbon/eventalreadypostederr?language=objc)
pub const eventAlreadyPostedErr: c_int = -9860;
/// [Apple's documentation](https://developer.apple.com/documentation/carbon/eventtargetbusyerr?language=objc)
pub const eventTargetBusyErr: c_int = -9861;
/// [Apple's documentation](https://developer.apple.com/documentation/carbon/eventclassinvaliderr?language=objc)
pub const eventClassInvalidErr: c_int = -9862;
/// [Apple's documentation](https://developer.apple.com/documentation/carbon/eventclassincorrecterr?language=objc)
pub const eventClassIncorrectErr: c_int = -9864;
/// [Apple's documentation](https://developer.apple.com/documentation/carbon/eventdeferaccessibilityeventerr?language=objc)
pub const eventDeferAccessibilityEventErr: c_int = -9865;
/// [Apple's documentation](https://developer.apple.com/documentation/carbon/eventhandleralreadyinstallederr?language=objc)
pub const eventHandlerAlreadyInstalledErr: c_int = -9866;
/// [Apple's documentation](https://developer.apple.com/documentation/carbon/eventinternalerr?language=objc)
pub const eventInternalErr: c_int = -9868;
/// [Apple's documentation](https://developer.apple.com/documentation/carbon/eventkindincorrecterr?language=objc)
pub const eventKindIncorrectErr: c_int = -9869;
/// [Apple's documentation](https://developer.apple.com/documentation/carbon/eventparameternotfounderr?language=objc)
pub const eventParameterNotFoundErr: c_int = -9870;
/// [Apple's documentation](https://developer.apple.com/documentation/carbon/eventnothandlederr?language=objc)
pub const eventNotHandledErr: c_int = -9874;
/// [Apple's documentation](https://developer.apple.com/documentation/carbon/eventlooptimedouterr?language=objc)
pub const eventLoopTimedOutErr: c_int = -9875;
/// [Apple's documentation](https://developer.apple.com/documentation/carbon/eventloopquiterr?language=objc)
pub const eventLoopQuitErr: c_int = -9876;
/// [Apple's documentation](https://developer.apple.com/documentation/carbon/eventnotinqueueerr?language=objc)
pub const eventNotInQueueErr: c_int = -9877;
/// [Apple's documentation](https://developer.apple.com/documentation/carbon/eventhotkeyexistserr?language=objc)
pub const eventHotKeyExistsErr: c_int = -9878;
/// [Apple's documentation](https://developer.apple.com/documentation/carbon/eventhotkeyinvaliderr?language=objc)
pub const eventHotKeyInvalidErr: c_int = -9879;
/// [Apple's documentation](https://developer.apple.com/documentation/carbon/eventpasstonexttargeterr?language=objc)
pub const eventPassToNextTargetErr: c_int = -9880;

/// [Apple's documentation](https://developer.apple.com/documentation/carbon/keventparamdirectobject?language=objc)
pub const kEventParamDirectObject: c_uint = 0x2d2d2d2d;
/// [Apple's documentation](https://developer.apple.com/documentation/carbon/keventparamdragref?language=objc)
pub const kEventParamDragRef: c_uint = 0x64726167;

/// [Apple's documentation](https://developer.apple.com/documentation/carbon/typedragref?language=objc)
pub const typeDragRef: c_uint = 0x64726167;
/// [Apple's documentation](https://developer.apple.com/documentation/carbon/typectfontref?language=objc)
pub const typeCTFontRef: c_uint = 0x63746672;
/// [Apple's documentation](https://developer.apple.com/documentation/carbon/typectglyphinforef?language=objc)
pub const typeCTGlyphInfoRef: c_uint = 0x63746769;

/// [Apple's documentation](https://developer.apple.com/documentation/carbon/eventpriority?language=objc)
pub type EventPriority = i16;

/// [Apple's documentation](https://developer.apple.com/documentation/carbon/keventprioritylow?language=objc)
pub const kEventPriorityLow: c_uint = 0;
/// [Apple's documentation](https://developer.apple.com/documentation/carbon/keventprioritystandard?language=objc)
pub const kEventPriorityStandard: c_uint = 1;
/// [Apple's documentation](https://developer.apple.com/documentation/carbon/keventpriorityhigh?language=objc)
pub const kEventPriorityHigh: c_uint = 2;

/// [Apple's documentation](https://developer.apple.com/documentation/carbon/keventleaveinqueue?language=objc)
pub const kEventLeaveInQueue: bool = false;
/// [Apple's documentation](https://developer.apple.com/documentation/carbon/keventremovefromqueue?language=objc)
pub const kEventRemoveFromQueue: bool = true;

/// [Apple's documentation](https://developer.apple.com/documentation/carbon/eventtime?language=objc)
pub type EventTime = c_double;

/// [Apple's documentation](https://developer.apple.com/documentation/carbon/eventtimeout?language=objc)
pub type EventTimeout = EventTime;

/// [Apple's documentation](https://developer.apple.com/documentation/carbon/eventtimerinterval?language=objc)
pub type EventTimerInterval = EventTime;

/// [Apple's documentation](https://developer.apple.com/documentation/carbon/eventtypespec?language=objc)
#[repr(C, packed(2))]
#[derive(Clone, Copy, Debug, PartialEq, Default)]
pub struct EventTypeSpec {
    pub eventClass: OSType,
    pub eventKind: u32,
}

#[cfg(feature = "objc2")]
unsafe impl Encode for EventTypeSpec {
    const ENCODING: Encoding =
        Encoding::Struct("EventTypeSpec", &[<OSType>::ENCODING, <u32>::ENCODING]);
}

#[cfg(feature = "objc2")]
unsafe impl RefEncode for EventTypeSpec {
    const ENCODING_REF: Encoding = Encoding::Pointer(&Self::ENCODING);
}

/// [Apple's documentation](https://developer.apple.com/documentation/carbon/eventparamname?language=objc)
pub type EventParamName = OSType;

/// [Apple's documentation](https://developer.apple.com/documentation/carbon/eventparamtype?language=objc)
pub type EventParamType = OSType;

/// [Apple's documentation](https://developer.apple.com/documentation/carbon/eventloop?language=objc)
#[doc(alias = "EventLoopRef")]
#[repr(C)]
#[derive(Debug)]
pub struct EventLoop {
    inner: [u8; 0],
    _p: UnsafeCell<PhantomData<(*const UnsafeCell<()>, PhantomPinned)>>,
}

#[cfg(feature = "objc2")]
unsafe impl RefEncode for EventLoop {
    const ENCODING_REF: Encoding = Encoding::Pointer(&Encoding::Struct("OpaqueEventLoopRef", &[]));
}

#[inline]
pub unsafe fn GetCurrentEventLoop() -> *mut EventLoop {
    extern "C-unwind" {
        fn GetCurrentEventLoop() -> *mut EventLoop;
    }
    unsafe { GetCurrentEventLoop() }
}

#[inline]
pub unsafe fn GetMainEventLoop() -> *mut EventLoop {
    extern "C-unwind" {
        fn GetMainEventLoop() -> *mut EventLoop;
    }
    unsafe { GetMainEventLoop() }
}

#[inline]
pub unsafe fn RunCurrentEventLoop(in_timeout: EventTimeout) -> OSStatus {
    extern "C-unwind" {
        fn RunCurrentEventLoop(in_timeout: EventTimeout) -> OSStatus;
    }
    unsafe { RunCurrentEventLoop(in_timeout) }
}

/// # Safety
///
/// - `in_event_loop` might need manual memory-management.
/// - `in_event_loop` might not allow `None`.
#[inline]
pub unsafe fn QuitEventLoop(in_event_loop: Option<&EventLoop>) -> OSStatus {
    extern "C-unwind" {
        fn QuitEventLoop(in_event_loop: Option<&EventLoop>) -> OSStatus;
    }
    unsafe { QuitEventLoop(in_event_loop) }
}

/// # Safety
///
/// - `in_event_loop` might need manual memory-management.
/// - `in_event_loop` might not allow `None`.
#[inline]
pub unsafe fn GetCFRunLoopFromEventLoop(
    in_event_loop: Option<&EventLoop>,
) -> Option<CFRetained<CFType>> {
    extern "C-unwind" {
        fn GetCFRunLoopFromEventLoop(in_event_loop: Option<&EventLoop>) -> Option<NonNull<CFType>>;
    }
    let ret = unsafe { GetCFRunLoopFromEventLoop(in_event_loop) };
    ret.map(|ret| unsafe { CFRetained::retain(ret) })
}

/// # Safety
///
/// - `in_list` must be a valid pointer.
/// - `out_event` must be a valid pointer.
/// - `out_event` might not allow `None`.
#[inline]
pub unsafe fn ReceiveNextEvent(
    in_num_types: ItemCount,
    in_list: *const EventTypeSpec,
    in_timeout: EventTimeout,
    in_pull_event: bool,
    out_event: Option<&mut *mut Event>,
) -> OSStatus {
    extern "C-unwind" {
        fn ReceiveNextEvent(
            in_num_types: ItemCount,
            in_list: *const EventTypeSpec,
            in_timeout: EventTimeout,
            in_pull_event: Boolean,
            out_event: Option<&mut *mut Event>,
        ) -> OSStatus;
    }
    let in_pull_event = in_pull_event as _;
    unsafe { ReceiveNextEvent(in_num_types, in_list, in_timeout, in_pull_event, out_event) }
}

/// [Apple's documentation](https://developer.apple.com/documentation/carbon/eventattributes?language=objc)
pub type EventAttributes = u32;

/// [Apple's documentation](https://developer.apple.com/documentation/carbon/keventattributenone?language=objc)
pub const kEventAttributeNone: c_uint = 0;
/// [Apple's documentation](https://developer.apple.com/documentation/carbon/keventattributeuserevent?language=objc)
pub const kEventAttributeUserEvent: c_uint = 1 << 0;
/// [Apple's documentation](https://developer.apple.com/documentation/carbon/keventattributemonitored?language=objc)
pub const kEventAttributeMonitored: c_uint = 1 << 3;

/// # Safety
///
/// - `out_event` must be a valid pointer.
/// - `out_event` might not allow `None`.
#[inline]
pub unsafe fn CreateEvent(
    in_allocator: Option<&CFAllocator>,
    in_class_id: OSType,
    in_kind: u32,
    in_when: EventTime,
    in_attributes: EventAttributes,
    out_event: Option<&mut *mut Event>,
) -> OSStatus {
    extern "C-unwind" {
        fn CreateEvent(
            in_allocator: Option<&CFAllocator>,
            in_class_id: OSType,
            in_kind: u32,
            in_when: EventTime,
            in_attributes: EventAttributes,
            out_event: Option<&mut *mut Event>,
        ) -> OSStatus;
    }
    unsafe {
        CreateEvent(
            in_allocator,
            in_class_id,
            in_kind,
            in_when,
            in_attributes,
            out_event,
        )
    }
}

/// # Safety
///
/// - `in_other` might need manual memory-management.
/// - `in_other` might not allow `None`.
#[inline]
pub unsafe fn CopyEvent(in_other: Option<&Event>) -> *mut Event {
    extern "C-unwind" {
        fn CopyEvent(in_other: Option<&Event>) -> *mut Event;
    }
    unsafe { CopyEvent(in_other) }
}

/// # Safety
///
/// - `in_other` might need manual memory-management.
/// - `in_other` might not allow `None`.
#[inline]
pub unsafe fn CopyEventAs(
    in_allocator: Option<&CFAllocator>,
    in_other: Option<&Event>,
    in_event_class: OSType,
    in_event_kind: u32,
) -> *mut Event {
    extern "C-unwind" {
        fn CopyEventAs(
            in_allocator: Option<&CFAllocator>,
            in_other: Option<&Event>,
            in_event_class: OSType,
            in_event_kind: u32,
        ) -> *mut Event;
    }
    unsafe { CopyEventAs(in_allocator, in_other, in_event_class, in_event_kind) }
}

/// # Safety
///
/// `in_event` must be a valid pointer.
#[inline]
pub unsafe fn RetainEvent(in_event: *mut Event) -> *mut Event {
    extern "C-unwind" {
        fn RetainEvent(in_event: *mut Event) -> *mut Event;
    }
    unsafe { RetainEvent(in_event) }
}

/// # Safety
///
/// `in_event` must be a valid pointer.
#[inline]
pub unsafe fn GetEventRetainCount(in_event: *mut Event) -> ItemCount {
    extern "C-unwind" {
        fn GetEventRetainCount(in_event: *mut Event) -> ItemCount;
    }
    unsafe { GetEventRetainCount(in_event) }
}

/// # Safety
///
/// `in_event` must be a valid pointer.
#[inline]
pub unsafe fn ReleaseEvent(in_event: *mut Event) {
    extern "C-unwind" {
        fn ReleaseEvent(in_event: *mut Event);
    }
    unsafe { ReleaseEvent(in_event) }
}

/// # Safety
///
/// - `in_event` might need manual memory-management.
/// - `in_event` might not allow `None`.
/// - `in_data_ptr` must be a valid pointer.
#[inline]
pub unsafe fn SetEventParameter(
    in_event: Option<&Event>,
    in_name: EventParamName,
    in_type: EventParamType,
    in_size: ByteCount,
    in_data_ptr: *const c_void,
) -> OSStatus {
    extern "C-unwind" {
        fn SetEventParameter(
            in_event: Option<&Event>,
            in_name: EventParamName,
            in_type: EventParamType,
            in_size: ByteCount,
            in_data_ptr: *const c_void,
        ) -> OSStatus;
    }
    unsafe { SetEventParameter(in_event, in_name, in_type, in_size, in_data_ptr) }
}

/// # Safety
///
/// - `in_event` might need manual memory-management.
/// - `in_event` might not allow `None`.
/// - `out_actual_type` might not allow `None`.
/// - `out_actual_size` might not allow `None`.
/// - `out_data` must be a valid pointer.
#[inline]
pub unsafe fn GetEventParameter(
    in_event: Option<&Event>,
    in_name: EventParamName,
    in_desired_type: EventParamType,
    out_actual_type: Option<&mut EventParamType>,
    in_buffer_size: ByteCount,
    out_actual_size: Option<&mut ByteCount>,
    out_data: *mut c_void,
) -> OSStatus {
    extern "C-unwind" {
        fn GetEventParameter(
            in_event: Option<&Event>,
            in_name: EventParamName,
            in_desired_type: EventParamType,
            out_actual_type: Option<&mut EventParamType>,
            in_buffer_size: ByteCount,
            out_actual_size: Option<&mut ByteCount>,
            out_data: *mut c_void,
        ) -> OSStatus;
    }
    unsafe {
        GetEventParameter(
            in_event,
            in_name,
            in_desired_type,
            out_actual_type,
            in_buffer_size,
            out_actual_size,
            out_data,
        )
    }
}

/// # Safety
///
/// - `in_event` might need manual memory-management.
/// - `in_event` might not allow `None`.
#[inline]
pub unsafe fn RemoveEventParameter(in_event: Option<&Event>, in_name: EventParamName) -> OSStatus {
    extern "C-unwind" {
        fn RemoveEventParameter(in_event: Option<&Event>, in_name: EventParamName) -> OSStatus;
    }
    unsafe { RemoveEventParameter(in_event, in_name) }
}

/// # Safety
///
/// - `in_event` might need manual memory-management.
/// - `in_event` might not allow `None`.
#[inline]
pub unsafe fn GetEventClass(in_event: Option<&Event>) -> OSType {
    extern "C-unwind" {
        fn GetEventClass(in_event: Option<&Event>) -> OSType;
    }
    unsafe { GetEventClass(in_event) }
}

/// # Safety
///
/// - `in_event` might need manual memory-management.
/// - `in_event` might not allow `None`.
#[inline]
pub unsafe fn GetEventKind(in_event: Option<&Event>) -> u32 {
    extern "C-unwind" {
        fn GetEventKind(in_event: Option<&Event>) -> u32;
    }
    unsafe { GetEventKind(in_event) }
}

/// # Safety
///
/// - `in_event` might need manual memory-management.
/// - `in_event` might not allow `None`.
#[inline]
pub unsafe fn GetEventTime(in_event: Option<&Event>) -> EventTime {
    extern "C-unwind" {
        fn GetEventTime(in_event: Option<&Event>) -> EventTime;
    }
    unsafe { GetEventTime(in_event) }
}

/// # Safety
///
/// - `in_event` might need manual memory-management.
/// - `in_event` might not allow `None`.
#[inline]
pub unsafe fn SetEventTime(in_event: Option<&Event>, in_time: EventTime) -> OSStatus {
    extern "C-unwind" {
        fn SetEventTime(in_event: Option<&Event>, in_time: EventTime) -> OSStatus;
    }
    unsafe { SetEventTime(in_event, in_time) }
}

/// # Safety
///
/// - `in_event` might not allow `None`.
/// - `out_event` must be a valid pointer.
/// - `out_event` might not allow `None`.
#[cfg(feature = "objc2-core-graphics")]
#[inline]
pub unsafe fn CreateEventWithCGEvent(
    in_allocator: Option<&CFAllocator>,
    in_event: Option<&CGEvent>,
    in_attributes: EventAttributes,
    out_event: Option<&mut *mut Event>,
) -> OSStatus {
    extern "C-unwind" {
        fn CreateEventWithCGEvent(
            in_allocator: Option<&CFAllocator>,
            in_event: Option<&CGEvent>,
            in_attributes: EventAttributes,
            out_event: Option<&mut *mut Event>,
        ) -> OSStatus;
    }
    unsafe { CreateEventWithCGEvent(in_allocator, in_event, in_attributes, out_event) }
}

/// # Safety
///
/// - `in_event` might need manual memory-management.
/// - `in_event` might not allow `None`.
#[cfg(feature = "objc2-core-graphics")]
#[inline]
pub unsafe fn CopyEventCGEvent(in_event: Option<&Event>) -> Option<CFRetained<CGEvent>> {
    extern "C-unwind" {
        fn CopyEventCGEvent(in_event: Option<&Event>) -> Option<NonNull<CGEvent>>;
    }
    let ret = unsafe { CopyEventCGEvent(in_event) };
    ret.map(|ret| unsafe { CFRetained::from_raw(ret) })
}

/// [Apple's documentation](https://developer.apple.com/documentation/carbon/eventqueue?language=objc)
#[doc(alias = "EventQueueRef")]
#[repr(C)]
#[derive(Debug)]
pub struct EventQueue {
    inner: [u8; 0],
    _p: UnsafeCell<PhantomData<(*const UnsafeCell<()>, PhantomPinned)>>,
}

#[cfg(feature = "objc2")]
unsafe impl RefEncode for EventQueue {
    const ENCODING_REF: Encoding = Encoding::Pointer(&Encoding::Struct("OpaqueEventQueueRef", &[]));
}

#[inline]
pub unsafe fn GetCurrentEventQueue() -> *mut EventQueue {
    extern "C-unwind" {
        fn GetCurrentEventQueue() -> *mut EventQueue;
    }
    unsafe { GetCurrentEventQueue() }
}

#[inline]
pub unsafe fn GetMainEventQueue() -> *mut EventQueue {
    extern "C-unwind" {
        fn GetMainEventQueue() -> *mut EventQueue;
    }
    unsafe { GetMainEventQueue() }
}

/// [Apple's documentation](https://developer.apple.com/documentation/carbon/eventcomparatorprocptr?language=objc)
pub type EventComparatorProcPtr =
    Option<unsafe extern "C-unwind" fn(*mut Event, *mut c_void) -> Boolean>;

/// [Apple's documentation](https://developer.apple.com/documentation/carbon/eventcomparatorupp?language=objc)
pub type EventComparatorUPP = EventComparatorProcPtr;

/// # Safety
///
/// `user_routine` must be implemented correctly.
#[inline]
pub unsafe fn NewEventComparatorUPP(user_routine: EventComparatorProcPtr) -> EventComparatorUPP {
    extern "C-unwind" {
        fn NewEventComparatorUPP(user_routine: EventComparatorProcPtr) -> EventComparatorUPP;
    }
    unsafe { NewEventComparatorUPP(user_routine) }
}

/// # Safety
///
/// `user_upp` must be implemented correctly.
#[inline]
pub unsafe fn DisposeEventComparatorUPP(user_upp: EventComparatorUPP) {
    extern "C-unwind" {
        fn DisposeEventComparatorUPP(user_upp: EventComparatorUPP);
    }
    unsafe { DisposeEventComparatorUPP(user_upp) }
}

/// # Safety
///
/// - `in_event` might need manual memory-management.
/// - `in_event` might not allow `None`.
/// - `in_compare_data` must be a valid pointer.
/// - `user_upp` must be implemented correctly.
#[inline]
pub unsafe fn InvokeEventComparatorUPP(
    in_event: Option<&Event>,
    in_compare_data: *mut c_void,
    user_upp: EventComparatorUPP,
) -> bool {
    extern "C-unwind" {
        fn InvokeEventComparatorUPP(
            in_event: Option<&Event>,
            in_compare_data: *mut c_void,
            user_upp: EventComparatorUPP,
        ) -> Boolean;
    }
    let ret = unsafe { InvokeEventComparatorUPP(in_event, in_compare_data, user_upp) };
    ret != 0
}

/// # Safety
///
/// - `in_queue` might need manual memory-management.
/// - `in_queue` might not allow `None`.
/// - `in_event` might need manual memory-management.
/// - `in_event` might not allow `None`.
#[inline]
pub unsafe fn PostEventToQueue(
    in_queue: Option<&EventQueue>,
    in_event: Option<&Event>,
    in_priority: EventPriority,
) -> OSStatus {
    extern "C-unwind" {
        fn PostEventToQueue(
            in_queue: Option<&EventQueue>,
            in_event: Option<&Event>,
            in_priority: EventPriority,
        ) -> OSStatus;
    }
    unsafe { PostEventToQueue(in_queue, in_event, in_priority) }
}

/// # Safety
///
/// - `in_queue` might need manual memory-management.
/// - `in_queue` might not allow `None`.
/// - `in_list` must be a valid pointer.
#[inline]
pub unsafe fn FlushEventsMatchingListFromQueue(
    in_queue: Option<&EventQueue>,
    in_num_types: ItemCount,
    in_list: *const EventTypeSpec,
) -> OSStatus {
    extern "C-unwind" {
        fn FlushEventsMatchingListFromQueue(
            in_queue: Option<&EventQueue>,
            in_num_types: ItemCount,
            in_list: *const EventTypeSpec,
        ) -> OSStatus;
    }
    unsafe { FlushEventsMatchingListFromQueue(in_queue, in_num_types, in_list) }
}

/// # Safety
///
/// - `in_queue` might need manual memory-management.
/// - `in_queue` might not allow `None`.
/// - `in_comparator` must be implemented correctly.
/// - `in_compare_data` must be a valid pointer.
#[inline]
pub unsafe fn FlushSpecificEventsFromQueue(
    in_queue: Option<&EventQueue>,
    in_comparator: EventComparatorUPP,
    in_compare_data: *mut c_void,
) -> OSStatus {
    extern "C-unwind" {
        fn FlushSpecificEventsFromQueue(
            in_queue: Option<&EventQueue>,
            in_comparator: EventComparatorUPP,
            in_compare_data: *mut c_void,
        ) -> OSStatus;
    }
    unsafe { FlushSpecificEventsFromQueue(in_queue, in_comparator, in_compare_data) }
}

/// # Safety
///
/// - `in_queue` might need manual memory-management.
/// - `in_queue` might not allow `None`.
#[inline]
pub unsafe fn FlushEventQueue(in_queue: Option<&EventQueue>) -> OSStatus {
    extern "C-unwind" {
        fn FlushEventQueue(in_queue: Option<&EventQueue>) -> OSStatus;
    }
    unsafe { FlushEventQueue(in_queue) }
}

/// # Safety
///
/// - `in_queue` might need manual memory-management.
/// - `in_queue` might not allow `None`.
/// - `in_comparator` must be implemented correctly.
/// - `in_compare_data` must be a valid pointer.
#[inline]
pub unsafe fn FindSpecificEventInQueue(
    in_queue: Option<&EventQueue>,
    in_comparator: EventComparatorUPP,
    in_compare_data: *mut c_void,
) -> *mut Event {
    extern "C-unwind" {
        fn FindSpecificEventInQueue(
            in_queue: Option<&EventQueue>,
            in_comparator: EventComparatorUPP,
            in_compare_data: *mut c_void,
        ) -> *mut Event;
    }
    unsafe { FindSpecificEventInQueue(in_queue, in_comparator, in_compare_data) }
}

/// # Safety
///
/// - `in_queue` might need manual memory-management.
/// - `in_queue` might not allow `None`.
#[inline]
pub unsafe fn GetNumEventsInQueue(in_queue: Option<&EventQueue>) -> ItemCount {
    extern "C-unwind" {
        fn GetNumEventsInQueue(in_queue: Option<&EventQueue>) -> ItemCount;
    }
    unsafe { GetNumEventsInQueue(in_queue) }
}

/// # Safety
///
/// - `in_queue` might need manual memory-management.
/// - `in_queue` might not allow `None`.
/// - `in_event` might need manual memory-management.
/// - `in_event` might not allow `None`.
#[inline]
pub unsafe fn RemoveEventFromQueue(
    in_queue: Option<&EventQueue>,
    in_event: Option<&Event>,
) -> OSStatus {
    extern "C-unwind" {
        fn RemoveEventFromQueue(
            in_queue: Option<&EventQueue>,
            in_event: Option<&Event>,
        ) -> OSStatus;
    }
    unsafe { RemoveEventFromQueue(in_queue, in_event) }
}

/// # Safety
///
/// - `in_queue` might need manual memory-management.
/// - `in_queue` might not allow `None`.
/// - `in_event` might need manual memory-management.
/// - `in_event` might not allow `None`.
#[inline]
pub unsafe fn IsEventInQueue(in_queue: Option<&EventQueue>, in_event: Option<&Event>) -> bool {
    extern "C-unwind" {
        fn IsEventInQueue(in_queue: Option<&EventQueue>, in_event: Option<&Event>) -> Boolean;
    }
    let ret = unsafe { IsEventInQueue(in_queue, in_event) };
    ret != 0
}

/// [Apple's documentation](https://developer.apple.com/documentation/carbon/keventqueueoptionsnone?language=objc)
pub const kEventQueueOptionsNone: c_uint = 0;

/// # Safety
///
/// - `in_queue` might need manual memory-management.
/// - `in_queue` might not allow `None`.
/// - `in_list` must be a valid pointer.
#[inline]
pub unsafe fn AcquireFirstMatchingEventInQueue(
    in_queue: Option<&EventQueue>,
    in_num_types: ItemCount,
    in_list: *const EventTypeSpec,
    in_options: OptionBits,
) -> *mut Event {
    extern "C-unwind" {
        fn AcquireFirstMatchingEventInQueue(
            in_queue: Option<&EventQueue>,
            in_num_types: ItemCount,
            in_list: *const EventTypeSpec,
            in_options: OptionBits,
        ) -> *mut Event;
    }
    unsafe { AcquireFirstMatchingEventInQueue(in_queue, in_num_types, in_list, in_options) }
}

#[inline]
pub unsafe fn GetCurrentEvent() -> *mut Event {
    extern "C-unwind" {
        fn GetCurrentEvent() -> *mut Event;
    }
    unsafe { GetCurrentEvent() }
}

#[inline]
pub unsafe fn GetCurrentEventButtonState() -> u32 {
    extern "C-unwind" {
        fn GetCurrentEventButtonState() -> u32;
    }
    unsafe { GetCurrentEventButtonState() }
}

#[inline]
pub unsafe fn GetCurrentEventKeyModifiers() -> u32 {
    extern "C-unwind" {
        fn GetCurrentEventKeyModifiers() -> u32;
    }
    unsafe { GetCurrentEventKeyModifiers() }
}

/// # Safety
///
/// - `in_object` must be a valid pointer.
/// - `out_point` might not allow `None`.
#[cfg(feature = "HIGeometry")]
#[inline]
pub unsafe fn HIGetMousePosition(
    in_space: HICoordinateSpace,
    in_object: *mut c_void,
    out_point: Option<&mut HIPoint>,
) -> *mut HIPoint {
    extern "C-unwind" {
        fn HIGetMousePosition(
            in_space: HICoordinateSpace,
            in_object: *mut c_void,
            out_point: Option<&mut HIPoint>,
        ) -> *mut HIPoint;
    }
    unsafe { HIGetMousePosition(in_space, in_object, out_point) }
}

#[inline]
pub unsafe fn GetCurrentButtonState() -> u32 {
    extern "C-unwind" {
        fn GetCurrentButtonState() -> u32;
    }
    unsafe { GetCurrentButtonState() }
}

#[inline]
pub unsafe fn GetCurrentKeyModifiers() -> u32 {
    extern "C-unwind" {
        fn GetCurrentKeyModifiers() -> u32;
    }
    unsafe { GetCurrentKeyModifiers() }
}

#[inline]
pub unsafe fn GetCurrentEventTime() -> EventTime {
    extern "C-unwind" {
        fn GetCurrentEventTime() -> EventTime;
    }
    unsafe { GetCurrentEventTime() }
}

/// [Apple's documentation](https://developer.apple.com/documentation/carbon/eventlooptimer?language=objc)
#[doc(alias = "EventLoopTimerRef")]
#[repr(C)]
pub struct EventLoopTimer {
    inner: [u8; 0],
    _p: UnsafeCell<PhantomData<(*const UnsafeCell<()>, PhantomPinned)>>,
}

cf_type!(
    unsafe impl EventLoopTimer {}
);
#[cfg(feature = "objc2")]
cf_objc2_type!(
    unsafe impl RefEncode<"__EventLoopTimer"> for EventLoopTimer {}
);

/// [Apple's documentation](https://developer.apple.com/documentation/carbon/eventlooptimerprocptr?language=objc)
pub type EventLoopTimerProcPtr =
    Option<unsafe extern "C-unwind" fn(*mut EventLoopTimer, *mut c_void)>;

/// [Apple's documentation](https://developer.apple.com/documentation/carbon/keventloopidletimerstarted?language=objc)
pub const kEventLoopIdleTimerStarted: c_uint = 1;
/// [Apple's documentation](https://developer.apple.com/documentation/carbon/keventloopidletimeridling?language=objc)
pub const kEventLoopIdleTimerIdling: c_uint = 2;
/// [Apple's documentation](https://developer.apple.com/documentation/carbon/keventloopidletimerstopped?language=objc)
pub const kEventLoopIdleTimerStopped: c_uint = 3;

/// [Apple's documentation](https://developer.apple.com/documentation/carbon/eventloopidletimermessage?language=objc)
pub type EventLoopIdleTimerMessage = u16;

/// [Apple's documentation](https://developer.apple.com/documentation/carbon/eventloopidletimerprocptr?language=objc)
pub type EventLoopIdleTimerProcPtr = Option<
    unsafe extern "C-unwind" fn(*mut EventLoopTimer, EventLoopIdleTimerMessage, *mut c_void),
>;

/// [Apple's documentation](https://developer.apple.com/documentation/carbon/eventlooptimerupp?language=objc)
pub type EventLoopTimerUPP = EventLoopTimerProcPtr;

/// [Apple's documentation](https://developer.apple.com/documentation/carbon/eventloopidletimerupp?language=objc)
pub type EventLoopIdleTimerUPP = EventLoopIdleTimerProcPtr;

/// # Safety
///
/// `user_routine` must be implemented correctly.
#[inline]
pub unsafe fn NewEventLoopTimerUPP(user_routine: EventLoopTimerProcPtr) -> EventLoopTimerUPP {
    extern "C-unwind" {
        fn NewEventLoopTimerUPP(user_routine: EventLoopTimerProcPtr) -> EventLoopTimerUPP;
    }
    unsafe { NewEventLoopTimerUPP(user_routine) }
}

/// # Safety
///
/// `user_routine` must be implemented correctly.
#[inline]
pub unsafe fn NewEventLoopIdleTimerUPP(
    user_routine: EventLoopIdleTimerProcPtr,
) -> EventLoopIdleTimerUPP {
    extern "C-unwind" {
        fn NewEventLoopIdleTimerUPP(
            user_routine: EventLoopIdleTimerProcPtr,
        ) -> EventLoopIdleTimerUPP;
    }
    unsafe { NewEventLoopIdleTimerUPP(user_routine) }
}

/// # Safety
///
/// `user_upp` must be implemented correctly.
#[inline]
pub unsafe fn DisposeEventLoopTimerUPP(user_upp: EventLoopTimerUPP) {
    extern "C-unwind" {
        fn DisposeEventLoopTimerUPP(user_upp: EventLoopTimerUPP);
    }
    unsafe { DisposeEventLoopTimerUPP(user_upp) }
}

/// # Safety
///
/// `user_upp` must be implemented correctly.
#[inline]
pub unsafe fn DisposeEventLoopIdleTimerUPP(user_upp: EventLoopIdleTimerUPP) {
    extern "C-unwind" {
        fn DisposeEventLoopIdleTimerUPP(user_upp: EventLoopIdleTimerUPP);
    }
    unsafe { DisposeEventLoopIdleTimerUPP(user_upp) }
}

/// # Safety
///
/// - `in_user_data` must be a valid pointer.
/// - `user_upp` must be implemented correctly.
#[inline]
pub unsafe fn InvokeEventLoopTimerUPP(
    in_timer: &EventLoopTimer,
    in_user_data: *mut c_void,
    user_upp: EventLoopTimerUPP,
) {
    extern "C-unwind" {
        fn InvokeEventLoopTimerUPP(
            in_timer: &EventLoopTimer,
            in_user_data: *mut c_void,
            user_upp: EventLoopTimerUPP,
        );
    }
    unsafe { InvokeEventLoopTimerUPP(in_timer, in_user_data, user_upp) }
}

/// # Safety
///
/// - `in_timer` might not allow `None`.
/// - `in_user_data` must be a valid pointer.
/// - `user_upp` must be implemented correctly.
#[inline]
pub unsafe fn InvokeEventLoopIdleTimerUPP(
    in_timer: Option<&EventLoopTimer>,
    in_state: EventLoopIdleTimerMessage,
    in_user_data: *mut c_void,
    user_upp: EventLoopIdleTimerUPP,
) {
    extern "C-unwind" {
        fn InvokeEventLoopIdleTimerUPP(
            in_timer: Option<&EventLoopTimer>,
            in_state: EventLoopIdleTimerMessage,
            in_user_data: *mut c_void,
            user_upp: EventLoopIdleTimerUPP,
        );
    }
    unsafe { InvokeEventLoopIdleTimerUPP(in_timer, in_state, in_user_data, user_upp) }
}

/// # Safety
///
/// - `in_event_loop` might need manual memory-management.
/// - `in_event_loop` might not allow `None`.
/// - `in_timer_proc` must be implemented correctly.
/// - `in_timer_data` must be a valid pointer.
/// - `out_timer` might not allow `None`.
#[inline]
pub unsafe fn InstallEventLoopTimer(
    in_event_loop: Option<&EventLoop>,
    in_fire_delay: EventTimerInterval,
    in_interval: EventTimerInterval,
    in_timer_proc: EventLoopTimerUPP,
    in_timer_data: *mut c_void,
    out_timer: Option<&mut Option<CFRetained<EventLoopTimer>>>,
) -> OSStatus {
    extern "C-unwind" {
        fn InstallEventLoopTimer(
            in_event_loop: Option<&EventLoop>,
            in_fire_delay: EventTimerInterval,
            in_interval: EventTimerInterval,
            in_timer_proc: EventLoopTimerUPP,
            in_timer_data: *mut c_void,
            out_timer: Option<&mut Option<CFRetained<EventLoopTimer>>>,
        ) -> OSStatus;
    }
    if let Some(out_timer) = out_timer.as_ref() {
        assert!(
            out_timer.is_none(),
            "parameter `out_timer` must point to `None` on entry"
        );
    };
    unsafe {
        InstallEventLoopTimer(
            in_event_loop,
            in_fire_delay,
            in_interval,
            in_timer_proc,
            in_timer_data,
            out_timer,
        )
    }
}

#[inline]
pub unsafe fn RemoveEventLoopTimer(in_timer: &EventLoopTimer) -> OSStatus {
    extern "C-unwind" {
        fn RemoveEventLoopTimer(in_timer: &EventLoopTimer) -> OSStatus;
    }
    unsafe { RemoveEventLoopTimer(in_timer) }
}

#[inline]
pub unsafe fn SetEventLoopTimerNextFireTime(
    in_timer: &EventLoopTimer,
    in_next_fire: EventTimerInterval,
) -> OSStatus {
    extern "C-unwind" {
        fn SetEventLoopTimerNextFireTime(
            in_timer: &EventLoopTimer,
            in_next_fire: EventTimerInterval,
        ) -> OSStatus;
    }
    unsafe { SetEventLoopTimerNextFireTime(in_timer, in_next_fire) }
}

/// [Apple's documentation](https://developer.apple.com/documentation/carbon/eventhandler?language=objc)
#[doc(alias = "EventHandlerRef")]
#[repr(C)]
#[derive(Debug)]
pub struct EventHandler {
    inner: [u8; 0],
    _p: UnsafeCell<PhantomData<(*const UnsafeCell<()>, PhantomPinned)>>,
}

#[cfg(feature = "objc2")]
unsafe impl RefEncode for EventHandler {
    const ENCODING_REF: Encoding =
        Encoding::Pointer(&Encoding::Struct("OpaqueEventHandlerRef", &[]));
}

/// [Apple's documentation](https://developer.apple.com/documentation/carbon/eventhandlercall?language=objc)
#[doc(alias = "EventHandlerCallRef")]
#[repr(C)]
#[derive(Debug)]
pub struct EventHandlerCall {
    inner: [u8; 0],
    _p: UnsafeCell<PhantomData<(*const UnsafeCell<()>, PhantomPinned)>>,
}

#[cfg(feature = "objc2")]
unsafe impl RefEncode for EventHandlerCall {
    const ENCODING_REF: Encoding =
        Encoding::Pointer(&Encoding::Struct("OpaqueEventHandlerCallRef", &[]));
}

/// [Apple's documentation](https://developer.apple.com/documentation/carbon/eventhandlerprocptr?language=objc)
pub type EventHandlerProcPtr =
    Option<unsafe extern "C-unwind" fn(*mut EventHandlerCall, *mut Event, *mut c_void) -> OSStatus>;

/// [Apple's documentation](https://developer.apple.com/documentation/carbon/eventhandlerupp?language=objc)
pub type EventHandlerUPP = EventHandlerProcPtr;

/// # Safety
///
/// `user_routine` must be implemented correctly.
#[inline]
pub unsafe fn NewEventHandlerUPP(user_routine: EventHandlerProcPtr) -> EventHandlerUPP {
    extern "C-unwind" {
        fn NewEventHandlerUPP(user_routine: EventHandlerProcPtr) -> EventHandlerUPP;
    }
    unsafe { NewEventHandlerUPP(user_routine) }
}

/// # Safety
///
/// `user_upp` must be implemented correctly.
#[inline]
pub unsafe fn DisposeEventHandlerUPP(user_upp: EventHandlerUPP) {
    extern "C-unwind" {
        fn DisposeEventHandlerUPP(user_upp: EventHandlerUPP);
    }
    unsafe { DisposeEventHandlerUPP(user_upp) }
}

/// # Safety
///
/// - `in_handler_call_ref` might need manual memory-management.
/// - `in_handler_call_ref` might not allow `None`.
/// - `in_event` might need manual memory-management.
/// - `in_event` might not allow `None`.
/// - `in_user_data` must be a valid pointer.
/// - `user_upp` must be implemented correctly.
#[inline]
pub unsafe fn InvokeEventHandlerUPP(
    in_handler_call_ref: Option<&EventHandlerCall>,
    in_event: Option<&Event>,
    in_user_data: *mut c_void,
    user_upp: EventHandlerUPP,
) -> OSStatus {
    extern "C-unwind" {
        fn InvokeEventHandlerUPP(
            in_handler_call_ref: Option<&EventHandlerCall>,
            in_event: Option<&Event>,
            in_user_data: *mut c_void,
            user_upp: EventHandlerUPP,
        ) -> OSStatus;
    }
    unsafe { InvokeEventHandlerUPP(in_handler_call_ref, in_event, in_user_data, user_upp) }
}

/// [Apple's documentation](https://developer.apple.com/documentation/carbon/eventtarget?language=objc)
#[doc(alias = "EventTargetRef")]
#[repr(C)]
#[derive(Debug)]
pub struct EventTarget {
    inner: [u8; 0],
    _p: UnsafeCell<PhantomData<(*const UnsafeCell<()>, PhantomPinned)>>,
}

#[cfg(feature = "objc2")]
unsafe impl RefEncode for EventTarget {
    const ENCODING_REF: Encoding =
        Encoding::Pointer(&Encoding::Struct("OpaqueEventTargetRef", &[]));
}

/// # Safety
///
/// - `in_target` might need manual memory-management.
/// - `in_target` might not allow `None`.
/// - `in_handler` must be implemented correctly.
/// - `in_list` must be a valid pointer.
/// - `in_user_data` must be a valid pointer.
/// - `out_ref` must be a valid pointer.
/// - `out_ref` might not allow `None`.
#[inline]
pub unsafe fn InstallEventHandler(
    in_target: Option<&EventTarget>,
    in_handler: EventHandlerUPP,
    in_num_types: ItemCount,
    in_list: *const EventTypeSpec,
    in_user_data: *mut c_void,
    out_ref: Option<&mut *mut EventHandler>,
) -> OSStatus {
    extern "C-unwind" {
        fn InstallEventHandler(
            in_target: Option<&EventTarget>,
            in_handler: EventHandlerUPP,
            in_num_types: ItemCount,
            in_list: *const EventTypeSpec,
            in_user_data: *mut c_void,
            out_ref: Option<&mut *mut EventHandler>,
        ) -> OSStatus;
    }
    unsafe {
        InstallEventHandler(
            in_target,
            in_handler,
            in_num_types,
            in_list,
            in_user_data,
            out_ref,
        )
    }
}

/// # Safety
///
/// - `in_handler_ref` might need manual memory-management.
/// - `in_handler_ref` might not allow `None`.
#[inline]
pub unsafe fn RemoveEventHandler(in_handler_ref: Option<&EventHandler>) -> OSStatus {
    extern "C-unwind" {
        fn RemoveEventHandler(in_handler_ref: Option<&EventHandler>) -> OSStatus;
    }
    unsafe { RemoveEventHandler(in_handler_ref) }
}

/// # Safety
///
/// - `in_handler_ref` might need manual memory-management.
/// - `in_handler_ref` might not allow `None`.
/// - `in_list` must be a valid pointer.
#[inline]
pub unsafe fn AddEventTypesToHandler(
    in_handler_ref: Option<&EventHandler>,
    in_num_types: ItemCount,
    in_list: *const EventTypeSpec,
) -> OSStatus {
    extern "C-unwind" {
        fn AddEventTypesToHandler(
            in_handler_ref: Option<&EventHandler>,
            in_num_types: ItemCount,
            in_list: *const EventTypeSpec,
        ) -> OSStatus;
    }
    unsafe { AddEventTypesToHandler(in_handler_ref, in_num_types, in_list) }
}

/// # Safety
///
/// - `in_handler_ref` might need manual memory-management.
/// - `in_handler_ref` might not allow `None`.
/// - `in_list` must be a valid pointer.
#[inline]
pub unsafe fn RemoveEventTypesFromHandler(
    in_handler_ref: Option<&EventHandler>,
    in_num_types: ItemCount,
    in_list: *const EventTypeSpec,
) -> OSStatus {
    extern "C-unwind" {
        fn RemoveEventTypesFromHandler(
            in_handler_ref: Option<&EventHandler>,
            in_num_types: ItemCount,
            in_list: *const EventTypeSpec,
        ) -> OSStatus;
    }
    unsafe { RemoveEventTypesFromHandler(in_handler_ref, in_num_types, in_list) }
}

/// # Safety
///
/// - `in_call_ref` might need manual memory-management.
/// - `in_call_ref` might not allow `None`.
/// - `in_event` might need manual memory-management.
/// - `in_event` might not allow `None`.
#[inline]
pub unsafe fn CallNextEventHandler(
    in_call_ref: Option<&EventHandlerCall>,
    in_event: Option<&Event>,
) -> OSStatus {
    extern "C-unwind" {
        fn CallNextEventHandler(
            in_call_ref: Option<&EventHandlerCall>,
            in_event: Option<&Event>,
        ) -> OSStatus;
    }
    unsafe { CallNextEventHandler(in_call_ref, in_event) }
}

/// [Apple's documentation](https://developer.apple.com/documentation/carbon/keventtargetdontpropagate?language=objc)
pub const kEventTargetDontPropagate: c_uint = 1 << 0;
/// [Apple's documentation](https://developer.apple.com/documentation/carbon/keventtargetsendtoallhandlers?language=objc)
pub const kEventTargetSendToAllHandlers: c_uint = 1 << 1;

/// # Safety
///
/// - `in_event` might need manual memory-management.
/// - `in_event` might not allow `None`.
/// - `in_target` might need manual memory-management.
/// - `in_target` might not allow `None`.
#[inline]
pub unsafe fn SendEventToEventTarget(
    in_event: Option<&Event>,
    in_target: Option<&EventTarget>,
) -> OSStatus {
    extern "C-unwind" {
        fn SendEventToEventTarget(
            in_event: Option<&Event>,
            in_target: Option<&EventTarget>,
        ) -> OSStatus;
    }
    unsafe { SendEventToEventTarget(in_event, in_target) }
}

/// # Safety
///
/// - `in_event` might need manual memory-management.
/// - `in_event` might not allow `None`.
/// - `in_target` might need manual memory-management.
/// - `in_target` might not allow `None`.
#[inline]
pub unsafe fn SendEventToEventTargetWithOptions(
    in_event: Option<&Event>,
    in_target: Option<&EventTarget>,
    in_options: OptionBits,
) -> OSStatus {
    extern "C-unwind" {
        fn SendEventToEventTargetWithOptions(
            in_event: Option<&Event>,
            in_target: Option<&EventTarget>,
            in_options: OptionBits,
        ) -> OSStatus;
    }
    unsafe { SendEventToEventTargetWithOptions(in_event, in_target, in_options) }
}

#[inline]
pub unsafe fn EnableSecureEventInput() -> OSStatus {
    extern "C-unwind" {
        fn EnableSecureEventInput() -> OSStatus;
    }
    unsafe { EnableSecureEventInput() }
}

#[inline]
pub unsafe fn DisableSecureEventInput() -> OSStatus {
    extern "C-unwind" {
        fn DisableSecureEventInput() -> OSStatus;
    }
    unsafe { DisableSecureEventInput() }
}

#[inline]
pub unsafe fn IsSecureEventInputEnabled() -> bool {
    extern "C-unwind" {
        fn IsSecureEventInputEnabled() -> Boolean;
    }
    let ret = unsafe { IsSecureEventInputEnabled() };
    ret != 0
}
