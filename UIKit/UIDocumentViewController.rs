//! This file has been automatically generated by `objc2`'s `header-translator`.
//! DO NOT EDIT
use core::ffi::*;
use core::ptr::NonNull;
use objc2::__framework_prelude::*;
use objc2_foundation::*;

use crate::*;

extern_class!(
    /// A view controller that manages and presents a document stored locally or in the cloud.
    ///
    /// ## Overview
    ///
    /// `UIDocumentViewController` presents a view that displays a [`document`](https://developer.apple.com/documentation/uikit/uidocumentviewcontroller/document). It manages opening, saving, and closing the document. You can use `UIDocumentViewController` to present documents from a [`UIDocumentBrowserViewController`](https://developer.apple.com/documentation/uikit/uidocumentbrowserviewcontroller), for instance. When you aren’t presenting documents from a `UIDocumentBrowserViewController`,  `UIDocumentViewController` displays a button that, when tapped, presents a picker a person can use to choose the document to display.
    ///
    /// The document view controller’s [`document`](https://developer.apple.com/documentation/uikit/uidocumentviewcontroller/document) property is an optional [`UIDocument`](https://developer.apple.com/documentation/uikit/uidocument). While the `document` property value is `nil`, the document view controller displays a view depicting an empty state with the message, “No Document”. The empty state provides a prompt that reads “Select a document by tapping the ‘Documents’ button at the top.”
    ///
    /// When the `document` property value is not `nil`, the document view controller displays the document. You configure the [`view`](https://developer.apple.com/documentation/uikit/uiviewcontroller/view) to display the document in the [`documentDidOpen`](https://developer.apple.com/documentation/uikit/uidocumentviewcontroller/documentdidopen()) callback method.
    ///
    /// The navigation item automatically assigns its title menu, its [`UIDocumentProperties`](https://developer.apple.com/documentation/uikit/uidocumentproperties) object, undo and redo buttons, and [`renameDelegate`](https://developer.apple.com/documentation/uikit/uinavigationitem/renamedelegate-o32h) from information in the document view controller’s `document`.
    ///
    /// `UIDocumentViewController` also provides sharing and drag and drop support, and key-commands for common actions such as undo and redo.
    ///
    /// ### Subclassing notes
    ///
    /// `UIDocumentViewController` is an abstract base class that is meant to be subclassed. To create a subclass, you override two methods.
    ///
    /// 1. The system calls [`documentDidOpen`](https://developer.apple.com/documentation/uikit/uidocumentviewcontroller/documentdidopen()) when someone opens the document associated with the document view controller, or when an object assigns a previously opened document to the document view controller.
    ///
    /// 2. The system calls [`navigationItemDidUpdate`](https://developer.apple.com/documentation/uikit/uidocumentviewcontroller/navigationitemdidupdate()) every time `UIDocumentViewController` makes changes to its navigation item.
    ///
    /// #### Open and close documents
    ///
    /// When the document view controller opens the document associated with it, or when an object assigns an already opened document to the document view controller, the system calls [`documentDidOpen`](https://developer.apple.com/documentation/uikit/uidocumentviewcontroller/documentdidopen()). You can populate the view controller’s views to display the content of the document in this method:
    ///
    /// ```swift
    /// override func documentDidOpen() {
    ///     configureViewForCurrentDocument()
    /// }
    /// ```
    ///
    /// It’s good practice to configure the view in its own method and call that method in both `documentDidOpen()` and [`viewDidLoad`](https://developer.apple.com/documentation/uikit/uiviewcontroller/viewdidload()). There is no guarantee of the timing between when the system calls `documentDidOpen()` and when the system loads the view controller’s view, so check that the view loaded and the document opened before configuring your views.
    ///
    /// ```swift
    /// override func viewDidLoad() {
    ///     super.viewDidLoad()
    ///     configureViewForCurrentDocument()
    /// }
    ///
    /// func configureViewForCurrentDocument() {
    ///     guard let document = markdownDocument,
    ///         !document.documentState.contains(.closed)
    ///             && isViewLoaded else { return }
    ///     // Configure views for document
    /// }
    /// ```
    ///
    /// #### Customize navigation items
    ///
    /// While `UIDocumentViewController` automatically sets the document title and other navigation items using information from the document, you can customize these items by overriding the [`navigationItemDidUpdate`](https://developer.apple.com/documentation/uikit/uidocumentviewcontroller/navigationitemdidupdate()) method. The system calls `navigationItemDidUpdate()` every time `UIDocumentViewController` makes changes to the navigation item.
    ///
    /// ```swift
    /// override func navigationItemDidUpdate() {
    ///     // Customize the navigation item.
    /// }
    /// ```
    ///
    /// #### Add sharing, drag and drop, and undo and redo
    ///
    /// `UIDocumentViewController` automatically includes drag and drop, undo and redo, and sharing support. If you want undo and redo buttons to appear in your `UIDocumentViewController`, add a [`undoRedoItemGroup`](https://developer.apple.com/documentation/uikit/uidocumentviewcontroller/undoredoitemgroup) to the navigation bar and ensure that your custom `UIDocument` implements an undo manager. `UIDocumentViewController` sets the `hidden` property of this group, depending on the availability of an undo manager. It automatically enables or disables the buttons inside the group, as necessary.
    ///
    /// #### Open a document from outside the class
    ///
    /// `UIDocumentViewController` automatically opens and closes its document. However, if you need to access a document from outside the document view controller, you can create a new instance of `UIDocumentViewController` and call its [`openDocumentWithCompletionHandler:`](https://developer.apple.com/documentation/uikit/uidocumentviewcontroller/opendocument(completionhandler:)) method.
    ///
    /// In this sample code, a [`UIDocumentBrowserViewController`](https://developer.apple.com/documentation/uikit/uidocumentbrowserviewcontroller) presents a document using a `UIDocumentViewController`.
    ///
    /// ```swift
    /// // Open a document.
    ///
    /// class BrowserViewController: UIDocumentBrowserViewController {
    ///
    ///     func presentDocument(_ document: MarkdownDocument) {
    ///         let documentController = UIDocumentViewController(document: document)
    ///         documentController.openDocument { success in
    ///             if success {
    ///                 self.present(documentController, animated: true)
    ///             }
    ///         }
    ///     }
    ///
    /// }
    /// ```
    ///
    /// `UIDocumentViewController` makes all the necessary callbacks, such as calling [`documentDidOpen`](https://developer.apple.com/documentation/uikit/uidocumentviewcontroller/documentdidopen()) and, when the document is open, it calls the completion handler you provided.
    ///
    /// #### Use the class as a root view controller
    ///
    /// `To use a UIDocumentViewController` as the root view controller in an app, you need to declare the key “UIDocumentClass” for the relevant file type in your app’s info.plist. Set the document class to the `UIDocument` subclass you are using in your app.
    ///
    /// If there is no browser view controller in the hierarchy, `UIDocumentViewController` displays a document button that opens a document picker in the navigation bar.
    ///
    ///
    #[unsafe(super(UIViewController, UIResponder, NSObject))]
    #[derive(Debug, PartialEq, Eq, Hash)]
    #[cfg(all(feature = "UIResponder", feature = "UIViewController"))]
    pub struct UIDocumentViewController;
);

#[cfg(all(feature = "UIResponder", feature = "UIViewController"))]
extern_conformance!(
    unsafe impl NSCoding for UIDocumentViewController {}
);

#[cfg(all(feature = "UIResponder", feature = "UIViewController"))]
extern_conformance!(
    unsafe impl NSObjectProtocol for UIDocumentViewController {}
);

#[cfg(all(
    feature = "UIAppearance",
    feature = "UIResponder",
    feature = "UIViewController"
))]
extern_conformance!(
    unsafe impl UIAppearanceContainer for UIDocumentViewController {}
);

#[cfg(all(feature = "UIResponder", feature = "UIViewController"))]
extern_conformance!(
    unsafe impl UIContentContainer for UIDocumentViewController {}
);

#[cfg(all(
    feature = "UIFocus",
    feature = "UIResponder",
    feature = "UIViewController"
))]
extern_conformance!(
    unsafe impl UIFocusEnvironment for UIDocumentViewController {}
);

#[cfg(all(feature = "UIResponder", feature = "UIViewController"))]
extern_conformance!(
    unsafe impl UIResponderStandardEditActions for UIDocumentViewController {}
);

#[cfg(all(
    feature = "UIResponder",
    feature = "UITraitCollection",
    feature = "UIViewController"
))]
extern_conformance!(
    unsafe impl UITraitEnvironment for UIDocumentViewController {}
);

#[cfg(all(feature = "UIResponder", feature = "UIViewController"))]
impl UIDocumentViewController {
    extern_methods!(
        #[cfg(feature = "UIDocument")]
        #[unsafe(method(initWithDocument:))]
        #[unsafe(method_family = init)]
        pub fn initWithDocument(
            this: Allocated<Self>,
            document: Option<&UIDocument>,
        ) -> Retained<Self>;

        #[cfg(feature = "UIDocument")]
        #[unsafe(method(document))]
        #[unsafe(method_family = none)]
        pub fn document(&self) -> Option<Retained<UIDocument>>;

        #[cfg(feature = "UIDocument")]
        /// Setter for [`document`][Self::document].
        #[unsafe(method(setDocument:))]
        #[unsafe(method_family = none)]
        pub fn setDocument(&self, document: Option<&UIDocument>);

        #[cfg(feature = "UIDocumentViewControllerLaunchOptions")]
        /// Properties to configure the view controller when no document is open.
        #[unsafe(method(launchOptions))]
        #[unsafe(method_family = none)]
        pub fn launchOptions(&self) -> Retained<UIDocumentViewControllerLaunchOptions>;

        #[cfg(feature = "UIDocumentViewControllerLaunchOptions")]
        /// Setter for [`launchOptions`][Self::launchOptions].
        #[unsafe(method(setLaunchOptions:))]
        #[unsafe(method_family = none)]
        pub fn setLaunchOptions(&self, launch_options: &UIDocumentViewControllerLaunchOptions);

        /// When this view controller updates its navigation item, this method will be called, allowing subclasses to apply any kind of customization you might want.
        #[unsafe(method(navigationItemDidUpdate))]
        #[unsafe(method_family = none)]
        pub fn navigationItemDidUpdate(&self);

        #[cfg(feature = "block2")]
        /// Opens the current document.
        /// This method is optional. If the document is not opened by the time the view controller becomes visible, the view controller will take care of opening the document.
        /// If the document is already opened, the completion handler will be called as if opening the document succeeded.
        #[unsafe(method(openDocumentWithCompletionHandler:))]
        #[unsafe(method_family = none)]
        pub fn openDocumentWithCompletionHandler(
            &self,
            completion_handler: &block2::DynBlock<dyn Fn(Bool)>,
        );

        /// Notifies subclasses that the specified document was opened. This method will be called by the system after
        /// opening the document initially or after the presented document was changed.
        #[unsafe(method(documentDidOpen))]
        #[unsafe(method_family = none)]
        pub fn documentDidOpen(&self);

        #[cfg(feature = "UIBarButtonItemGroup")]
        /// MARK: Custom Navigation Items
        #[unsafe(method(undoRedoItemGroup))]
        #[unsafe(method_family = none)]
        pub fn undoRedoItemGroup(&self) -> Retained<UIBarButtonItemGroup>;
    );
}

/// Methods declared on superclass `UIViewController`.
#[cfg(all(feature = "UIResponder", feature = "UIViewController"))]
impl UIDocumentViewController {
    extern_methods!(
        #[unsafe(method(initWithNibName:bundle:))]
        #[unsafe(method_family = init)]
        pub fn initWithNibName_bundle(
            this: Allocated<Self>,
            nib_name_or_nil: Option<&NSString>,
            nib_bundle_or_nil: Option<&NSBundle>,
        ) -> Retained<Self>;

        /// # Safety
        ///
        /// `coder` possibly has further requirements.
        #[unsafe(method(initWithCoder:))]
        #[unsafe(method_family = init)]
        pub unsafe fn initWithCoder(
            this: Allocated<Self>,
            coder: &NSCoder,
        ) -> Option<Retained<Self>>;
    );
}

/// Methods declared on superclass `NSObject`.
#[cfg(all(feature = "UIResponder", feature = "UIViewController"))]
impl UIDocumentViewController {
    extern_methods!(
        #[unsafe(method(init))]
        #[unsafe(method_family = init)]
        pub fn init(this: Allocated<Self>) -> Retained<Self>;

        #[unsafe(method(new))]
        #[unsafe(method_family = new)]
        pub fn new(mtm: MainThreadMarker) -> Retained<Self>;
    );
}
