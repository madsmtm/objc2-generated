//! This file has been automatically generated by `objc2`'s `header-translator`.
//! DO NOT EDIT
use core::ffi::*;
use core::ptr::NonNull;
use objc2::__framework_prelude::*;

use crate::*;

/// Executes the script with no input and ignoring any result.
///
/// This method should be invoked no more than once for a given instance of the class. If the script completed normally, the completion handler's `error` parameter will be `nil`.
///
/// - Parameters:
/// - handler: The completion handler Block that returns the error, or `nil` if no error occurred.
/// Implement this block to retrieve the error of the script executed by `executeWithCompletionHandler:`.
///
/// See also [Apple's documentation](https://developer.apple.com/documentation/foundation/nsuserscripttaskcompletionhandler?language=objc)
#[cfg(all(feature = "NSError", feature = "block2"))]
pub type NSUserScriptTaskCompletionHandler = block2::SendableBlock<'static, fn(*mut NSError)>;

extern_class!(
    /// An object that executes scripts.
    ///
    /// The ``NSUserScriptTask`` class is able to run all the scripts normally run by the one of its subclasses, however it ignores the results. It is intended to execute user-supplied scripts and will execute them outside of the application's sandbox, if any.
    ///
    /// If you need to execute scripts and get the input and output information use the ``NSUserUnixTask``, ``NSUserAppleScriptTask``, and ``NSUserAutomatorTask`` sub classes.
    ///
    /// See also [Apple's documentation](https://developer.apple.com/documentation/foundation/nsuserscripttask?language=objc)
    #[unsafe(super(NSObject))]
    #[derive(Debug, PartialEq, Eq, Hash)]
    pub struct NSUserScriptTask;
);

extern_conformance!(
    unsafe impl NSObjectProtocol for NSUserScriptTask {}
);

impl NSUserScriptTask {
    extern_methods!(
        #[cfg(all(feature = "NSError", feature = "NSURL"))]
        /// Initializes a script task with the given URL for a script file.
        ///
        /// The returned object will be of one of the specific sub-classes (``NSUserUnixTask``, ``NSUserAppleScriptTask``, or ``NSUserAutomatorTask``), or `nil` if the file does not appear to match any of the known types.
        ///
        /// If invoked from a subclass, the result will be that class or `nil`.
        ///
        /// - Parameters:
        /// - url: The URL for the script file.
        /// - error: On return, an error object if an error occurred, otherwise `nil`.
        /// - Returns: An initialized script task, or `nil` if the file does not match a known type.
        #[unsafe(method(initWithURL:error:_))]
        #[unsafe(method_family = init)]
        pub fn initWithURL_error(
            this: Allocated<Self>,
            url: &NSURL,
        ) -> Result<Retained<Self>, Retained<NSError>>;

        #[cfg(feature = "NSURL")]
        /// The URL of the script file.
        #[unsafe(method(scriptURL))]
        #[unsafe(method_family = none)]
        pub fn scriptURL(&self) -> Retained<NSURL>;

        #[cfg(all(feature = "NSError", feature = "block2"))]
        #[unsafe(method(executeWithCompletionHandler:))]
        #[unsafe(method_family = none)]
        pub fn executeWithCompletionHandler(
            &self,
            handler: Option<&NSUserScriptTaskCompletionHandler>,
        );
    );
}

/// Methods declared on superclass `NSObject`.
impl NSUserScriptTask {
    extern_methods!(
        #[unsafe(method(init))]
        #[unsafe(method_family = init)]
        pub fn init(this: Allocated<Self>) -> Retained<Self>;

        #[unsafe(method(new))]
        #[unsafe(method_family = new)]
        pub fn new() -> Retained<Self>;
    );
}

impl DefaultRetained for NSUserScriptTask {
    #[inline]
    fn default_retained() -> Retained<Self> {
        Self::new()
    }
}

/// Executes the unix script with the specified arguments.
///
/// This method should be invoked no more than once for a given instance of the class. If the script completed normally, the completion handler's `error` parameter will be `nil`.
///
/// - Parameters:
/// - arguments: An array of `NSString` objects containing the script arguments. The arguments do not undergo shell expansion, so you do not need to do special quoting, and shell variables are not resolved.
/// - handler: The completion handler Block that returns the error, or `nil` if no error occurred.
/// Implement this block to retrieve an error from the Unix script executed by `executeWithArguments:completionHandler:`.
///
/// See also [Apple's documentation](https://developer.apple.com/documentation/foundation/nsuserunixtaskcompletionhandler?language=objc)
#[cfg(all(feature = "NSError", feature = "block2"))]
pub type NSUserUnixTaskCompletionHandler = block2::SendableBlock<'static, fn(*mut NSError)>;

extern_class!(
    /// An object that executes unix applications.
    ///
    /// The ``NSUserUnixTask`` class is intended to run unix applications, typically a shell script, from your application. It is intended to execute user-supplied scripts, and will execute them outside of the application's sandbox, if any.
    ///
    /// The class is not intended to execute scripts built into an application; for that, use one of the ``Process``, ``NSAppleScript``, or
    /// <doc
    /// ://com.apple.documentation/documentation/automator/amworkflow> classes.  If the application is sandboxed, then the script must be in the ``FileManager/SearchPathDirectory/applicationScriptsDirectory`` folder.  A sandboxed application may read from, but not write to, this folder.
    ///
    /// If you simply need to execute unix scripts without regard to input or output, use ``NSUserScriptTask``, which can execute any of the specific types.  If you need specific control over the input to, or output from, or the error stream of the script, use this class.
    ///
    /// See also [Apple's documentation](https://developer.apple.com/documentation/foundation/nsuserunixtask?language=objc)
    #[unsafe(super(NSUserScriptTask, NSObject))]
    #[derive(Debug, PartialEq, Eq, Hash)]
    pub struct NSUserUnixTask;
);

extern_conformance!(
    unsafe impl NSObjectProtocol for NSUserUnixTask {}
);

impl NSUserUnixTask {
    extern_methods!(
        #[cfg(feature = "NSFileHandle")]
        /// The standard input stream.
        ///
        /// Setting to `nil` will bind the stream to `/dev/null`. The default is `nil`.
        #[unsafe(method(standardInput))]
        #[unsafe(method_family = none)]
        pub fn standardInput(&self) -> Option<Retained<NSFileHandle>>;

        #[cfg(feature = "NSFileHandle")]
        /// Setter for [`standardInput`][Self::standardInput].
        #[unsafe(method(setStandardInput:))]
        #[unsafe(method_family = none)]
        pub fn setStandardInput(&self, standard_input: Option<&NSFileHandle>);

        #[cfg(feature = "NSFileHandle")]
        /// The standard output stream.
        ///
        /// Setting to `nil` will bind the stream to `/dev/null`. The default is `nil`.
        #[unsafe(method(standardOutput))]
        #[unsafe(method_family = none)]
        pub fn standardOutput(&self) -> Option<Retained<NSFileHandle>>;

        #[cfg(feature = "NSFileHandle")]
        /// Setter for [`standardOutput`][Self::standardOutput].
        #[unsafe(method(setStandardOutput:))]
        #[unsafe(method_family = none)]
        pub fn setStandardOutput(&self, standard_output: Option<&NSFileHandle>);

        #[cfg(feature = "NSFileHandle")]
        /// The standard error stream.
        ///
        /// Setting to `nil` will bind the stream to `/dev/null`. The default is `nil`.
        #[unsafe(method(standardError))]
        #[unsafe(method_family = none)]
        pub fn standardError(&self) -> Option<Retained<NSFileHandle>>;

        #[cfg(feature = "NSFileHandle")]
        /// Setter for [`standardError`][Self::standardError].
        #[unsafe(method(setStandardError:))]
        #[unsafe(method_family = none)]
        pub fn setStandardError(&self, standard_error: Option<&NSFileHandle>);

        #[cfg(all(
            feature = "NSArray",
            feature = "NSError",
            feature = "NSString",
            feature = "block2"
        ))]
        #[unsafe(method(executeWithArguments:completionHandler:))]
        #[unsafe(method_family = none)]
        pub fn executeWithArguments_completionHandler(
            &self,
            arguments: Option<&NSArray<NSString>>,
            handler: Option<&NSUserUnixTaskCompletionHandler>,
        );
    );
}

/// Methods declared on superclass `NSUserScriptTask`.
impl NSUserUnixTask {
    extern_methods!(
        #[cfg(all(feature = "NSError", feature = "NSURL"))]
        /// Initializes a script task with the given URL for a script file.
        ///
        /// The returned object will be of one of the specific sub-classes (``NSUserUnixTask``, ``NSUserAppleScriptTask``, or ``NSUserAutomatorTask``), or `nil` if the file does not appear to match any of the known types.
        ///
        /// If invoked from a subclass, the result will be that class or `nil`.
        ///
        /// - Parameters:
        /// - url: The URL for the script file.
        /// - error: On return, an error object if an error occurred, otherwise `nil`.
        /// - Returns: An initialized script task, or `nil` if the file does not match a known type.
        #[unsafe(method(initWithURL:error:_))]
        #[unsafe(method_family = init)]
        pub fn initWithURL_error(
            this: Allocated<Self>,
            url: &NSURL,
        ) -> Result<Retained<Self>, Retained<NSError>>;
    );
}

/// Methods declared on superclass `NSObject`.
impl NSUserUnixTask {
    extern_methods!(
        #[unsafe(method(init))]
        #[unsafe(method_family = init)]
        pub fn init(this: Allocated<Self>) -> Retained<Self>;

        #[unsafe(method(new))]
        #[unsafe(method_family = new)]
        pub fn new() -> Retained<Self>;
    );
}

impl DefaultRetained for NSUserUnixTask {
    #[inline]
    fn default_retained() -> Retained<Self> {
        Self::new()
    }
}

/// Executes the AppleScript script by sending it the given Apple event.
///
/// Pass `nil` as `event` to execute the script's default "run" handler. This method should be invoked no more than once for a given instance of the class. If the script completed normally, the completion handler's `error` parameter will be `nil`.
///
/// - Parameters:
/// - event: The Apple event to send to the script. Pass `nil` to execute the script's default "run" handler.
/// - handler: The completion handler Block that returns the result or an error.
/// Implement this block to retrieve the result of the AppleScript executed by `executeWithAppleEvent:completionHandler:`.
///
/// See also [Apple's documentation](https://developer.apple.com/documentation/foundation/nsuserapplescripttaskcompletionhandler?language=objc)
#[cfg(all(
    feature = "NSAppleEventDescriptor",
    feature = "NSError",
    feature = "block2"
))]
pub type NSUserAppleScriptTaskCompletionHandler =
    block2::SendableBlock<'static, fn(*mut NSAppleEventDescriptor, *mut NSError)>;

extern_class!(
    /// An object that executes AppleScript scripts.
    ///
    /// The ``NSUserAppleScriptTask`` class is intended to run AppleScript scripts from your application. It is intended to execute user-supplied scripts and will execute them outside of the application's sandbox, if any.
    ///
    /// The class is not intended to execute scripts built into an application; for that, use one of the ``Process`` classes. If the application is sandboxed, then the script must be in the ``FileManager/SearchPathDirectory/applicationScriptsDirectory`` folder. A sandboxed application may read from, but not write to, this folder.
    ///
    /// If you simply need to execute scripts without regard to input or output, use ``NSUserScriptTask``, which can execute any of the specific types. If you need specific control over the input to or output from the script, use this class.
    ///
    /// See also [Apple's documentation](https://developer.apple.com/documentation/foundation/nsuserapplescripttask?language=objc)
    #[unsafe(super(NSUserScriptTask, NSObject))]
    #[derive(Debug, PartialEq, Eq, Hash)]
    pub struct NSUserAppleScriptTask;
);

extern_conformance!(
    unsafe impl NSObjectProtocol for NSUserAppleScriptTask {}
);

impl NSUserAppleScriptTask {
    extern_methods!(
        #[cfg(all(
            feature = "NSAppleEventDescriptor",
            feature = "NSError",
            feature = "block2"
        ))]
        #[unsafe(method(executeWithAppleEvent:completionHandler:))]
        #[unsafe(method_family = none)]
        pub fn executeWithAppleEvent_completionHandler(
            &self,
            event: Option<&NSAppleEventDescriptor>,
            handler: Option<&NSUserAppleScriptTaskCompletionHandler>,
        );
    );
}

/// Methods declared on superclass `NSUserScriptTask`.
impl NSUserAppleScriptTask {
    extern_methods!(
        #[cfg(all(feature = "NSError", feature = "NSURL"))]
        /// Initializes a script task with the given URL for a script file.
        ///
        /// The returned object will be of one of the specific sub-classes (``NSUserUnixTask``, ``NSUserAppleScriptTask``, or ``NSUserAutomatorTask``), or `nil` if the file does not appear to match any of the known types.
        ///
        /// If invoked from a subclass, the result will be that class or `nil`.
        ///
        /// - Parameters:
        /// - url: The URL for the script file.
        /// - error: On return, an error object if an error occurred, otherwise `nil`.
        /// - Returns: An initialized script task, or `nil` if the file does not match a known type.
        #[unsafe(method(initWithURL:error:_))]
        #[unsafe(method_family = init)]
        pub fn initWithURL_error(
            this: Allocated<Self>,
            url: &NSURL,
        ) -> Result<Retained<Self>, Retained<NSError>>;
    );
}

/// Methods declared on superclass `NSObject`.
impl NSUserAppleScriptTask {
    extern_methods!(
        #[unsafe(method(init))]
        #[unsafe(method_family = init)]
        pub fn init(this: Allocated<Self>) -> Retained<Self>;

        #[unsafe(method(new))]
        #[unsafe(method_family = new)]
        pub fn new() -> Retained<Self>;
    );
}

impl DefaultRetained for NSUserAppleScriptTask {
    #[inline]
    fn default_retained() -> Retained<Self> {
        Self::new()
    }
}

/// Executes the Automator workflow, passing it the given input.
///
/// The Automator workflow will execute using the ``variables`` property values. This method should be invoked no more than once for a given instance of the class. If the script completed normally, the completion handler's `error` parameter will be `nil`.
///
/// - Parameters:
/// - input: The input to pass to the Automator workflow.
/// - handler: The completion handler Block that returns the result or an error.
/// Implement this block to retrieve the output of the Automator workflow executed by `executeWithInput:completionHandler:`.
///
/// See also [Apple's documentation](https://developer.apple.com/documentation/foundation/nsuserautomatortaskcompletionhandler?language=objc)
#[cfg(all(feature = "NSError", feature = "block2"))]
pub type NSUserAutomatorTaskCompletionHandler =
    block2::SendableBlock<'static, fn(*mut AnyObject, *mut NSError)>;

extern_class!(
    /// An object that executes Automator workflows.
    ///
    /// The ``NSUserAutomatorTask`` class is intended to run Automator workflows from your application. It is intended to execute user-supplied workflows, and will execute them outside of the application's sandbox, if any.
    ///
    /// The class is not intended to execute scripts built into an application; for that, use one of the ``Process`` or
    /// <doc
    /// ://com.apple.documentation/documentation/automator/amworkflow> classes.  If the application is sandboxed, then the script must be in the ``FileManager/SearchPathDirectory/applicationScriptsDirectory`` folder.  A sandboxed application may read from, but not write to, this folder.
    ///
    /// If you simply need to execute scripts without regard to input or output, use ``NSUserScriptTask``, which can execute any of the specific types.  If you need specific control over the input to or output from the workflow, use this class.
    ///
    /// See also [Apple's documentation](https://developer.apple.com/documentation/foundation/nsuserautomatortask?language=objc)
    #[unsafe(super(NSUserScriptTask, NSObject))]
    #[derive(Debug, PartialEq, Eq, Hash)]
    pub struct NSUserAutomatorTask;
);

extern_conformance!(
    unsafe impl NSObjectProtocol for NSUserAutomatorTask {}
);

impl NSUserAutomatorTask {
    extern_methods!(
        #[cfg(all(feature = "NSDictionary", feature = "NSString"))]
        /// The variables required by the Automator workflow.
        #[unsafe(method(variables))]
        #[unsafe(method_family = none)]
        pub fn variables(&self) -> Option<Retained<NSDictionary<NSString, AnyObject>>>;

        #[cfg(all(feature = "NSDictionary", feature = "NSString"))]
        /// Setter for [`variables`][Self::variables].
        ///
        /// This is [copied][crate::NSCopying::copy] when set.
        ///
        /// # Safety
        ///
        /// `variables` generic should be of the correct type.
        #[unsafe(method(setVariables:))]
        #[unsafe(method_family = none)]
        pub unsafe fn setVariables(&self, variables: Option<&NSDictionary<NSString, AnyObject>>);

        #[cfg(all(feature = "NSError", feature = "NSObject", feature = "block2"))]
        /// # Safety
        ///
        /// `input` should be of the correct type.
        #[unsafe(method(executeWithInput:completionHandler:))]
        #[unsafe(method_family = none)]
        pub unsafe fn executeWithInput_completionHandler(
            &self,
            input: Option<&ProtocolObject<dyn NSSecureCoding>>,
            handler: Option<&NSUserAutomatorTaskCompletionHandler>,
        );
    );
}

/// Methods declared on superclass `NSUserScriptTask`.
impl NSUserAutomatorTask {
    extern_methods!(
        #[cfg(all(feature = "NSError", feature = "NSURL"))]
        /// Initializes a script task with the given URL for a script file.
        ///
        /// The returned object will be of one of the specific sub-classes (``NSUserUnixTask``, ``NSUserAppleScriptTask``, or ``NSUserAutomatorTask``), or `nil` if the file does not appear to match any of the known types.
        ///
        /// If invoked from a subclass, the result will be that class or `nil`.
        ///
        /// - Parameters:
        /// - url: The URL for the script file.
        /// - error: On return, an error object if an error occurred, otherwise `nil`.
        /// - Returns: An initialized script task, or `nil` if the file does not match a known type.
        #[unsafe(method(initWithURL:error:_))]
        #[unsafe(method_family = init)]
        pub fn initWithURL_error(
            this: Allocated<Self>,
            url: &NSURL,
        ) -> Result<Retained<Self>, Retained<NSError>>;
    );
}

/// Methods declared on superclass `NSObject`.
impl NSUserAutomatorTask {
    extern_methods!(
        #[unsafe(method(init))]
        #[unsafe(method_family = init)]
        pub fn init(this: Allocated<Self>) -> Retained<Self>;

        #[unsafe(method(new))]
        #[unsafe(method_family = new)]
        pub fn new() -> Retained<Self>;
    );
}

impl DefaultRetained for NSUserAutomatorTask {
    #[inline]
    fn default_retained() -> Retained<Self> {
        Self::new()
    }
}
